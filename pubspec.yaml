name: calculator_invest_flutter
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  mask_text_input_formatter: ^2.0.0
  intl: ^0.17.0
  google_fonts: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/invest.png
